class News {
  String category;
  String imagePath;
  String description;
  String createdDate;
  int numberOfViews;
  News(this.category, this.imagePath, this.description, this.createdDate,
      this.numberOfViews);
}
